# inbox — 다른 프로젝트에서 올라온 노하우

이 플러그인을 **쓰는** 프로젝트에서 겪은 일을, 이 플러그인을 **고치는** 세션으로
넘기는 한 방향 수신함. 적는 곳은 여기 하나, 판정하는 곳도 여기 하나다.

## 적는 법

파일 끝에 항목을 덧붙인다. 네 줄이면 된다 — 양식을 지키는 것보다 **적어두는 것**이 중요하다.

```markdown
## 2026-09-19 · 출처: <프로젝트 이름 또는 경로>
- **무슨 일**: 실제로 벌어진 일. 관찰된 사실만 — 원인 추정은 다음 줄에.
- **제안**: 플러그인이 어떻게 달라져야 하는가. 모르겠으면 "모름"이라고 적는다.
- **상태**: 대기
```

- **`무슨 일`에는 재현 가능한 것을 적는다** — 어떤 스킬/에이전트/훅이었는지, 무엇을 기대했고 무엇이 나왔는지.
  여기 적히는 세션은 이 저장소를 못 본 세션이므로, "그때 그거"는 나중에 아무도 못 찾는다.
- **한 항목 = 한 가지 일.** 세 가지를 겪었으면 항목 세 개다.

## 처리하는 법 (이 저장소 세션)

1. `상태: 대기` 항목을 읽는다.
2. 같은 얘기가 이미 `rules/` · `docs/` · `CLAUDE.md` 에 있는지 먼저 본다 — 대개 있다. 있으면 **반려**.
3. 고칠 값이 있으면 고치고, 상태를 갱신한다:
   - `반영됨 (<커밋 해시>)` — 커밋이 없는 반영은 반영이 아니다.
   - `반려 (<한 줄 이유>)` — 왜 안 고쳤는지 남겨야 같은 항목이 두 번 안 올라온다.
   - `보류 (<막힌 것>)` — 결정·데이터·권한이 없어서 못 한 것. 실패가 아니다.
4. **처리된 항목은 지우지 않는다.** 반려 이유가 곧 이 플러그인의 설계 기록이다.

항목 하나가 규칙 한 줄로 끝나면 여기서 바로 고친다. 여러 스킬·문서를 건드려야 하면
`/lesson-log` 로 레슨을 먼저 남기고 쪼갠다.

---

<!-- 여기부터 항목. 오래된 것이 위, 새 것이 아래. -->
